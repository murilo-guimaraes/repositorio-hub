const input=document.getElementById("searchInput"),buttons=document.querySelectorAll(".filter-button"),cards=document.querySelectorAll(".game-card"),empty=document.getElementById("emptyMessage");let category="todos";function updateGames(){const query=input.value.toLowerCase().trim();let visible=0;cards.forEach(card=>{const show=(category==="todos"||card.dataset.category===category)&&card.dataset.title.includes(query);card.style.display=show?"block":"none";if(show)visible++});empty.style.display=visible?"none":"block"}
input.addEventListener("input",updateGames);buttons.forEach(button=>button.addEventListener("click",()=>{buttons.forEach(item=>item.classList.remove("active"));button.classList.add("active");category=button.dataset.category;updateGames()}));